workflows:
  build:
    name: Build
    instance_type: mac_mini_m2
    environment:
      flutter: stable
    scripts:
      - flutter create --org com.vpn.app --project-name vpn .
      - cd vpn && flutter build apk --release
    artifacts:
      - vpn/build/app/outputs/flutter-apk/*.apk
