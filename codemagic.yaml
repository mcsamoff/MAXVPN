workflows:
  build:
    name: Build
    instance_type: mac_mini_m2
    environment:
      flutter: stable
    scripts:
      - flutter create --org com.vpn.app --project-name myapp .
      - cd myapp && flutter build apk --release
    artifacts:
      - myapp/build/app/outputs/flutter-apk/*.apk
